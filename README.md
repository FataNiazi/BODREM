# Experiments
## Manipulation
### To Run Manipulation Experiments:
```bash
python3 manipulation/run_ablations.py
```
The script will run all experiments in the `EXPERIMENTS` dictionary inside `manipulation/run_ablations.py`. Each experiment takes two arguments, the first being a method builder and the second a difficulity dictionary. The details for each can be found in the code itself. 